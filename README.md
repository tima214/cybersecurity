~~~
1 install openssl 
after downloading openssl 
pip install openssl -- no mask
cd file path where is openssl --- C:\PS.....
openssl version 
and where openssl 
who new work certificate ---- cert.pem

PEM -- base inform kroptografing 
-in --- also C++ amd python the programing 
where openssl not scripts and codec , the write save program the algorithms also is command this concole --->
           everything about cybersecurity the openssl and notepad----- good mine ----->
                    save and code and scrips the algorithm .ps amd .bath
on them .ps and bath complite scripd and code ---->
      good and run mask PowerShell and CMD -- but lenguage [c] 

all on good
~~~
